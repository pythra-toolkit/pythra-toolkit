# Core PyThra Framework Dependencies
PySide6>=6.10.0
pyyaml>=6.0
setuptools>=65.0
orjson>=3.11.4


# CLI Dependencies
typer>=0.12.0
rich>=13.0.0

# Package Management System Dependencies
semver>=3.0.0
requests>=2.31.0

# Development Dependencies (built-in modules, no install needed)
# uuid, cython, importlib, threading, inspect, weakref
# sys, os, time, socketserver, http
